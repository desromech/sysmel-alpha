namespace GPU definition: {

// GPU Graphics pipeline common interface
shader GraphicsPipeline interface definition: {

    // Vertex Stage intrinsics
    stageio VertexStage vertex definition: {
        output screenPosition type: Float32x4; location: -1.

        input vertexID type: Int32; location: -43.
        input instanceID type: Int32; location: -43.
    }.
}.

} // End of namespace GPU
