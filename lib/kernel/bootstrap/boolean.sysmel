namespace SysmelKernel definition: {

_CompilerObjectType macro expansionRule or: orCondition := ``(
    if: `,self then: true else: (`,orCondition __macroInlineBlock castTo: Boolean8)
).

_CompilerObjectType macro expansionRule || orCondition := ``(
    if: `,self then: true else: (`,orCondition __macroInlineBlock castTo: Boolean8)
).

_CompilerObjectType macro expansionRule and: andCondition := ``(
    if: `,self then: (`,andCondition __macroInlineBlock castTo: Boolean8) else: false
).

_CompilerObjectType macro expansionRule && andCondition := ``(
    if: `,self then: (`,andCondition __macroInlineBlock castTo: Boolean8) else: false
).

}. ## End of namespace SysmelKernel
