namespace Unix definition: {
    useNamespaceAndExportSymbols: CTypes.

    compileTimeConstant UID_T := UInt32.
    compileTimeConstant GID_T := UInt32.
    compileTimeConstant PID_T := Int32.
    compileTimeConstant OFF_T := IntPointer.
    compileTimeConstant OFF64_T := Int64.

}.
