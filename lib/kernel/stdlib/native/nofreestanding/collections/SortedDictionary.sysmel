namespace NStd definition: {
namespace Collections definition: {

template SortedDictionary(KT: Type, VT: Type)
    := class definition: {
    compileTimeConstant KeyType := KT.
    compileTimeConstant ValueType := VT.
    compileTimeConstant AssociationType := Association(KeyType, ValueType).
    ##compileTimeConstant NodeType := ExplicitMemoryCollections BalancedTreeNode(AssociationType).
}.

}. ## End of namespace Collections
}. ## End of namespace NStd
