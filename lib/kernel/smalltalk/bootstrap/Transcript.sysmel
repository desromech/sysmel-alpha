namespace GCStd definition: {

## TODO: Use a global variable here.
gcclass Transcript definition: {
    meta definition: {
        method show: anObject
            := {NStd stdout << anObject . self}.
        method cr
            := {NStd stdout cr . self}.
        method nl
            := {NStd stdout nl . self}.
    }
}.

}. ## End of namespace GCStd.
