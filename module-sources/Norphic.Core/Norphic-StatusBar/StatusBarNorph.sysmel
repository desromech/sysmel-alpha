namespace Norphic definition: {

#**
 * I am a status bar norph.
 *#
class StatusBarNorph superclass: Norph; definition: {
    override method defaultColor => Float32x4
        := self theme _ statusBarBackground.

    override method minimalContentExtent => Float32x2
        := Float32x2(0.0f, 10.0f).
}.

compileTime constant StatusBarNorphPtr := StatusBarNorph sharedPointer.

}. ## End of namespace Norphic
