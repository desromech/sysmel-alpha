namespace Norphic definition: {

useNamespace: Std Graphics GUI.

#**
 * I am a multi-line text area editor norph.
 *#
class TextAreaNorph superclass: AbstractTextEditorNorph; definition: {
    override method createTextEditingArea => Void := {
        super createTextEditingArea.
        textEditingArea isMultiLineEditor: true.
    }.
}.

compileTime constant TextAreaNorphRef := TextAreaNorph rcRef.

}. ## End of namespace Norphic
