namespace Smalltalk definition: {

gcclass ClassTestCase superclass: AbstractEnvironmentTestCase; definition: {
    method defaultTestError := {
        ## TODO:
        self.
    }.
}.

}. ## End of namespace Smalltalk
