namespace Smalltalk definition: {

gcclass Mutex superclass: Object; definition: {
    field handle type: Std RecursiveMutex.

    method critical: aBlock := {
        handle withLock: {
            aBlock value
        }
    }.
}.

}. ## End of namespace Smalltalk
