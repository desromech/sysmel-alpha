namespace Smalltalk definition: {

## TODO: Use a global variable here.
gcclass Transcript definition: {
    meta definition: {
        method show: anObject
            := {Std stdout << anObject . self}.
        method cr
            := {Std stdout cr . self}.
        method nl
            := {Std stdout nl . self}.
    }
}.

}. ## End of namespace Smalltalk.
