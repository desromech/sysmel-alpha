namespace Smalltalk definition: {

gcclass VirtualMachine superclass: Object; definition: {
    method wordSize
        := UIntPointer instanceSize.

    method isSpur
        := true.

    method supportsWriteBarrier
        := false.
}.

}. ## End of namespace Smalltalk
