namespace Smalltalk definition: {

gcclass TestResource superclass: TestAsserter; definition: {
    field testSelector.
    field expectedFails.

    meta definition: {
        method current := {
            self new setUp; yourself
        }
    }.

    method setUp := self.
    method tearDown := self.
}.

}. ## End of namespace Smalltalk
