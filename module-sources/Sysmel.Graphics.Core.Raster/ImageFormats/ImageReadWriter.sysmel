namespace Stdn definition: {
namespace Graphics definition: {
namespace ImageFormats definition: {

useNamespace: Stdn Graphics Core.

class ImageReadWriter superclass: Object; definition: {
    virtual const method supportsExtension: (extension: Stdn String const ref) ::=> Boolean8
        := false.

    virtual const method putForm: (form: Form ref) onFileNamed: (fileName: Stdn String const ref) ::=> Boolean8 := {
        let outFile mutable type: Stdn IO UnbufferedFileStream.
        (outFile open: fileName mode: Stdn IO FileOpenMode CreateNew) ifTrue: {
            self putForm: form onStream: outFile.
            return: true
        }.
        return: false
    }.

    virtual const method putForm: (form: Form ref) onStream: (outputStream: Stdn IO Stream ref) ::=> Void := {
        ## TODO: implement this.
    }.
}.

} ## End of namespace ImageFormats
} ## End of namespace Graphics
} ## End of namespace Stdn
