compileTime if: Compiler compilationTarget isLinux || Compiler compilationTarget isOSX then: {
    Compiler addLinkerArguments: #("-lpng16").
}.

Compiler
    importModulesNamed: #(
        #"Sysmel.Graphics.Core.Raster"
    );
    loadPackages: #(
        #"Bindings"
        #"ImageReadWriter"
    ).
