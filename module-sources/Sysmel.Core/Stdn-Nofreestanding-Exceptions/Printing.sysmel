namespace Stdn definition: {

namespace IO definition: {
    ## Opaque declarations do not make any harm.
    class TextStreamWriter.
}.

Exception extend: {
    const virtual method printOnNativeTextStreamWriter: (writer: Stdn IO TextStreamWriter ref) ::=> Void := {
        messageText ifNotNil: {
            writer << messageText _
        }
    }.
}.

}. ## End of namespace Stdn
