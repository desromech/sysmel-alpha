Compiler importModuleNamed: #"Sysmel.Devel.PharoBridge".

Stdn MainEntryPoint: {:args :: Int32 |
    let server := Smalltalk PharoBridgeServer new.
    server start.

    server mainLoop.

    return: 0.
}.
