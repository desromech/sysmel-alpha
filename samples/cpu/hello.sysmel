function main externC (argc: Int32, argv: Char8 const pointer pointer) => Int32 := {
    Stdn stdout << "Hello World " << 42; nl.

    Stdn stdout << "Argc " << argc; nl.
	1 until: argc do: {:(Int32)i :: Void |
        Stdn stdout << "Argv[" << i << "] := " << argv[i]; nl.
	}.

    return: 0.
}.
